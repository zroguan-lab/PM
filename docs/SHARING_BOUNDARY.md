# 共享与隐私边界

## 已纳入

- 源码、测试、产品说明、设计说明、启动脚本、依赖锁文件。
- W40 / OS 的四张阶段截图，便于快速理解界面成果。
- 顾问所需的事实化项目状态、开发入口和验证记录。

## 已排除

- 所有 `.env` 和凭证类文件。
- PM-BTC 的 `data/`、`logs/`、`run/`、`artifacts/`、Python 环境及前端缓存/产物。
- 投资决策复盘系统的 `data/` 和 `investment-review.db*`，避免共享个人投资记录。
- 所有项目的依赖目录与构建缓存。
- W40 / OS 的重复打包文件 `W40_OS_Codex_Pack.zip`。
- `封面`、`SKILL`：属于内容/视觉资产集合，不是本轮顾问协作的独立产品代码。
- `粒子`：扫描时为空目录。
- `mirofish`：较早的第三方实验副本，当前目录含本地 `.env`，且没有近期推进证据。

## 扫描结论

上传前对拟纳入文本源码做了凭证特征检查：未发现私钥块、GitHub token、OpenAI key、JWT 或带值的常见凭证赋值。该检查不能替代后续人工审查；新增文件仍需遵守根目录 `.gitignore` 和 [协作约定](../CONTRIBUTING.md)。

